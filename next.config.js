/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "oaidalleapiprodscus.blob.core.windows.net",
      "api.writesonic.com",
    ],
  },
};

module.exports = nextConfig;
